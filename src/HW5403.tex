\documentclass[letterpaper,11pt]{article}

\usepackage{amsmath, amsfonts, amssymb, amsthm}
\usepackage{lmodern}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage[x11names, rgb]{xcolor}
\usepackage{graphicx}
\usepackage{fullpage}
\usepackage{etoolbox}

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

\newcommand{\nopagenumbers}{
    \pagestyle{empty}
}

\newenvironment{indented}{
    \begin{list}{}{\leftmargin=1em}
    \item[]
}{
    \end{list}
}

\newtoggle{questionnumbers}
\toggletrue{questionnumbers}

\newcounter{questionCounter}
\newcounter{partCounter}[questionCounter]

\newcommand{\question}[1]{%
    \stepcounter{questionCounter}%
    \setcounter{partCounter}{0}%
    \vspace{0.25in}
    \hrule
    \vspace{0.4em}
    \noindent\textbf{Question \thequestionCounter:\ }#1
    \vspace{0.8em}
    \hrule
    \vspace{0.1in}
}

\newcommand{\qpart}{%
    \stepcounter{partCounter}%
    \begin{indented}
    \textbf{(\alph{partCounter}) }
}

\newcommand{\closepart}{
    \end{indented}
}

\newcommand{\myhwname}{AMATH 403 HW 5}
\newcommand{\myname}{Jordan Tucker}

\newcommand{\header}{
    \begin{center}
    {\Large \myhwname}\\
    \myname\\
    \today
    \end{center}
}

\nopagenumbers

\begin{document}

    \header
    \question{4.3.18. Use separation of variables to solve the Helmholtz boundary value problem
        $\Delta u =u $,  $u(x, 0) = 0$, $u(x, 1) = f (x)$, $u(0, y) = 0$, $u(1, y) = 0$, on the unit square $ 0 < x$, $y < 1$.}

    \textbf{Using separation of variables:}
    \[
        u(x,y) = v(x)w(y)
    \]
    \textbf{Helmholtz equation:}
    \begin{gather*}
        \Delta u = u \\
        u_{xx} + u_{yy} = u
    \end{gather*}
    \textbf{Using substitution:}
    \begin{gather*}
        u_{xx} = v''(x)w(y)\\
        u_{yy} = v(x)w''(y)\\
        v''(x)w(y) + v(x)w''(y) = v(x)w(y)
    \end{gather*}
    \textbf{Divide by $v(x)w(y)$}
    \begin{gather*}
    \end{gather*}
\end{document}